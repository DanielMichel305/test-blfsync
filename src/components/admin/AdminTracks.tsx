import React, { useEffect, useState } from 'react';
import { Archive, ArrowLeft, ExternalLink, Plus, Save, Trash2, X } from 'lucide-react';
import type { components } from '../../api/generated';
import { type TrackListParams, useCreateTrack, useDeleteTrack, useMinistryTracks, useSubscription, useTrack, useTrackCommitments, useUpdateTrack } from '../../api/hooks';
import { AdminListControls, AdminListState, AdminPagination, FilterField, useAdminListState } from './AdminList';
import { ConfirmDialog } from './ConfirmDialog';

type Track = components['schemas']['MinistryTrack'];
type TrackInput = components['schemas']['CreateMinistryTrackRequest'];
type Period = TrackInput['target_period'];
type Unit = TrackInput['metricUnit'];

const emptyTrack: TrackInput = { name: '', description: '', current_metric_level: 0, target_metric_level: 1, min_monthly_contribution: 1, cost_per_unit: 1, metricUnit: 'souls', target_period: 'Annually', isActive: true };

function validate(input: TrackInput) {
  if (!input.name.trim()) return 'Name is required.';
  if (input.description && input.description.length > 500) return 'Description must be 500 characters or fewer.';
  if (input.cover_url) { try { new URL(input.cover_url); } catch { return 'Cover URL must be a valid absolute URL.'; } }
  if (!Number.isFinite(input.current_metric_level) || input.current_metric_level < 0) return 'Current metric must be zero or greater.';
  if (!Number.isInteger(input.target_metric_level) || input.target_metric_level <= 0) return 'Target metric must be a positive whole number.';
  if (!Number.isInteger(input.min_monthly_contribution) || input.min_monthly_contribution < 0) return 'Minimum contribution must be zero or a positive whole number.';
  if (!Number.isInteger(input.cost_per_unit) || input.cost_per_unit <= 0) return 'Cost per unit must be a positive whole number.';
  return '';
}

function TrackForm({ initial, submitLabel, busy, onSubmit, onCancel }: { initial: TrackInput; submitLabel: string; busy: boolean; onSubmit: (input: TrackInput) => Promise<void>; onCancel?: () => void }) {
  const [form, setForm] = useState<TrackInput>(initial);
  const [error, setError] = useState('');
  useEffect(() => setForm(initial), [initial]);
  const field = <K extends keyof TrackInput>(key: K, value: TrackInput[K]) => setForm(current => ({ ...current, [key]: value }));
  return <form onSubmit={async event => { event.preventDefault(); const issue = validate(form); setError(issue); if (!issue) await onSubmit({ ...form, name: form.name.trim(), description: form.description?.trim() || undefined, cover_url: form.cover_url?.trim() || undefined }); }} className="grid gap-3 rounded-2xl border border-editorial-charcoal/10 bg-editorial-card p-5 md:grid-cols-2">
    <label className="text-xs font-bold">Name<input required maxLength={100} value={form.name} onChange={e => field('name', e.target.value)} className="mt-1 w-full rounded-xl border bg-transparent p-2 font-normal" /></label>
    <label className="text-xs font-bold">Cover URL<input type="url" value={form.cover_url || ''} onChange={e => field('cover_url', e.target.value)} className="mt-1 w-full rounded-xl border bg-transparent p-2 font-normal" /></label>
    <label className="text-xs font-bold md:col-span-2">Description<textarea maxLength={500} value={form.description || ''} onChange={e => field('description', e.target.value)} rows={3} className="mt-1 w-full rounded-xl border bg-transparent p-2 font-normal" /></label>
    {([['Current metric', 'current_metric_level', 0, '0.01'], ['Target metric', 'target_metric_level', 1, '1'], ['Minimum contribution', 'min_monthly_contribution', 0, '1'], ['Cost per unit', 'cost_per_unit', 1, '1']] as const).map(([label, key, min, step]) => <label key={key} className="text-xs font-bold">{label}<input required type="number" min={min} step={step} value={form[key]} onChange={e => field(key, Number(e.target.value))} className="mt-1 w-full rounded-xl border bg-transparent p-2 font-normal" /></label>)}
    <label className="text-xs font-bold">Metric unit<select value={form.metricUnit} onChange={e => field('metricUnit', e.target.value as Unit)} className="mt-1 w-full rounded-xl border bg-editorial-card p-2 font-normal">{(['souls', 'streams', 'usd', 'egp', 'houses built'] as Unit[]).map(value => <option key={value}>{value}</option>)}</select></label>
    <label className="text-xs font-bold">Target period<select value={form.target_period} onChange={e => field('target_period', e.target.value as Period)} className="mt-1 w-full rounded-xl border bg-editorial-card p-2 font-normal">{(['Monthly', 'Quarterly', 'Annually'] as Period[]).map(value => <option key={value}>{value}</option>)}</select></label>
    <label className="flex items-center gap-2 text-xs font-bold"><input type="checkbox" checked={form.isActive} onChange={e => field('isActive', e.target.checked)} /> Active</label>
    {error && <p role="alert" className="text-xs text-rose-600 md:col-span-2">{error}</p>}
    <div className="flex justify-end gap-2 md:col-span-2">{onCancel && <button type="button" onClick={onCancel} className="rounded-full border px-4 py-2 text-xs"><X className="mr-1 inline h-3 w-3" />Cancel</button>}<button disabled={busy} className="rounded-full bg-editorial-charcoal px-4 py-2 text-xs font-bold text-editorial-cream disabled:opacity-40"><Save className="mr-1 inline h-3 w-3" />{submitLabel}</button></div>
  </form>;
}

export function AdminTracks({ selectedTrackId, onOpenTrack, onCloseTrack }: { selectedTrackId?: string; onOpenTrack: (id: string) => void; onCloseTrack: () => void }) {
  if (selectedTrackId) return <AdminTrackDetail id={selectedTrackId} onBack={onCloseTrack} />;
  return <AdminTrackList onOpenTrack={onOpenTrack} />;
}

function AdminTrackList({ onOpenTrack }: { onOpenTrack: (id: string) => void }) {
  const { query, update } = useAdminListState('tracks', { page: 1, limit: 20, search: '', isActive: '', sortBy: 'createdAt', sortOrder: 'ASC' });
  const params = { ...query, isActive: query.isActive === '' ? undefined : query.isActive === 'true' };
  const list = useMinistryTracks(params as TrackListParams);
  const create = useCreateTrack(); const save = useUpdateTrack(); const remove = useDeleteTrack();
  const [creating, setCreating] = useState(false); const [editing, setEditing] = useState<Track | null>(null); const [confirm, setConfirm] = useState<{ track: Track; action: 'delete' | 'deactivate' } | null>(null); const [error, setError] = useState('');
  const run = async (operation: () => Promise<unknown>) => { setError(''); try { await operation(); setCreating(false); setEditing(null); setConfirm(null); } catch (cause) { setError(cause instanceof Error ? cause.message : 'Track action failed.'); } };
  return <div className="space-y-4">
    <div className="flex items-center justify-between"><div><h2 className="text-2xl font-serif">Ministry tracks</h2><p className="text-xs text-editorial-charcoal/50">Manage impact metrics, giving constraints, and reporting periods.</p></div><button onClick={() => setCreating(value => !value)} className="rounded-full bg-editorial-charcoal px-4 py-2 text-xs font-bold text-editorial-cream"><Plus className="mr-1 inline h-4 w-4" />Create ministry track</button></div>
    {creating && <TrackForm initial={emptyTrack} submitLabel="Create track" busy={create.isPending} onCancel={() => setCreating(false)} onSubmit={input => run(() => create.mutateAsync(input))} />}
    <AdminListControls query={query} onChange={update}><FilterField label="Active state"><select value={query.isActive} onChange={e => update({ isActive: e.target.value })}><option value="">All</option><option value="true">Active</option><option value="false">Inactive</option></select></FilterField><FilterField label="Sort by"><select value={query.sortBy} onChange={e => update({ sortBy: e.target.value })}>{['createdAt', 'isActive', 'Alphabetical', 'current_metric_level', 'target_metric_level', 'min_monthly_contribution'].map(value => <option key={value}>{value}</option>)}</select></FilterField><FilterField label="Order"><select value={query.sortOrder} onChange={e => update({ sortOrder: e.target.value })}><option>ASC</option><option>DESC</option></select></FilterField></AdminListControls>
    {error && <p role="alert" className="rounded-xl bg-rose-500/10 p-3 text-xs text-rose-600">{error}</p>}
    <AdminListState loading={list.isLoading} error={list.error} empty={!list.data?.ministryTracks.length}><div className="space-y-3">{list.data?.ministryTracks.map(track => editing?.id === track.id ? <TrackForm key={track.id} initial={{ name: track.name, description: track.description, cover_url: track.cover_url || undefined, current_metric_level: track.current_metric_level, target_metric_level: track.target_metric_level, min_monthly_contribution: track.min_monthly_contribution, cost_per_unit: track.cost_per_unit, metricUnit: track.metricUnit, target_period: track.target_period, isActive: track.isActive }} submitLabel="Save changes" busy={save.isPending} onCancel={() => setEditing(null)} onSubmit={input => run(() => save.mutateAsync({ id: track.id, input }))} /> : <article key={track.id} role="link" tabIndex={0} onClick={() => onOpenTrack(track.id)} onKeyDown={event => { if (event.key === 'Enter' || event.key === ' ') onOpenTrack(track.id); }} className="cursor-pointer rounded-2xl border border-editorial-charcoal/10 bg-editorial-card p-5 transition hover:border-editorial-charcoal/25 hover:shadow-sm"><div className="flex flex-wrap items-start gap-3"><div className="min-w-0 flex-1"><h3 className="font-serif text-lg">{track.name}</h3><p className="text-xs text-editorial-charcoal/55">{track.current_metric_level.toLocaleString()} / {track.target_metric_level.toLocaleString()} {track.metricUnit} · {track.target_period} · {track.isActive ? 'Active' : 'Inactive'}</p><p className="mt-2 text-sm text-editorial-charcoal/65">{track.description}</p><p className="mt-2 text-[10px] text-editorial-charcoal/45">Minimum ${track.min_monthly_contribution} · ${track.cost_per_unit} per unit</p></div><button onClick={event => { event.stopPropagation(); setEditing(track); }} className="rounded-full border px-3 py-1.5 text-xs">Edit</button>{track.isActive && <button title="Deactivate" onClick={event => { event.stopPropagation(); setConfirm({ track, action: 'deactivate' }); }} className="p-2 text-amber-700"><Archive className="h-4 w-4" /></button>}<button title="Delete" onClick={event => { event.stopPropagation(); setConfirm({ track, action: 'delete' }); }} className="p-2 text-rose-600"><Trash2 className="h-4 w-4" /></button><ExternalLink className="h-4 w-4 text-editorial-charcoal/35" /></div></article>)}</div></AdminListState>
    {list.data && <AdminPagination page={list.data.page} limit={list.data.limit} total={list.data.total} onChange={update} />}
    <ConfirmDialog open={!!confirm} title={confirm?.action === 'delete' ? 'Delete ministry track?' : 'Deactivate ministry track?'} description={confirm?.action === 'delete' ? 'This permanently removes the track if the API allows it. This cannot be undone.' : 'The track will no longer be active, but its records will remain available.'} confirmLabel={confirm?.action === 'delete' ? 'Delete track' : 'Deactivate'} destructive busy={save.isPending || remove.isPending} onCancel={() => setConfirm(null)} onConfirm={() => confirm && run(() => confirm.action === 'delete' ? remove.mutateAsync(confirm.track.id) : save.mutateAsync({ id: confirm.track.id, input: { isActive: false } }))} />
  </div>;
}

function AdminTrackDetail({ id, onBack }: { id: string; onBack: () => void }) {
  const track = useTrack(id);
  const { query, update } = useAdminListState('track_commitments', { page: 1, limit: 20, search: '' });
  const commitments = useTrackCommitments(id, { page: query.page, limit: query.limit });
  if (track.isLoading) return <AdminListState loading error={null} empty={false}><span /></AdminListState>;
  if (track.error || !track.data) return <div className="space-y-4"><button onClick={onBack} className="text-xs"><ArrowLeft className="mr-1 inline h-4 w-4" />Back to tracks</button><AdminListState loading={false} error={track.error || new Error('Track not found.')} empty={false}><span /></AdminListState></div>;
  const item = track.data;
  return <div className="space-y-5">
    <button onClick={onBack} className="text-xs"><ArrowLeft className="mr-1 inline h-4 w-4" />Back to tracks</button>
    <section className="overflow-hidden rounded-3xl border border-editorial-charcoal/10 bg-editorial-card">{item.cover_url && <img src={item.cover_url} alt="" className="h-52 w-full object-cover" />}<div className="p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[9px] font-bold uppercase tracking-widest text-editorial-charcoal/45">Track detail</p><h2 className="mt-1 font-serif text-3xl">{item.name}</h2></div><span className={`rounded-full px-3 py-1 text-[10px] font-bold uppercase ${item.isActive ? 'bg-emerald-500/10 text-emerald-700' : 'bg-rose-500/10 text-rose-700'}`}>{item.isActive ? 'Active' : 'Inactive'}</span></div><p className="mt-4 max-w-3xl text-sm leading-relaxed text-editorial-charcoal/65">{item.description || 'No description provided.'}</p><dl className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[['Current metric', item.current_metric_level.toLocaleString()], ['Target metric', `${item.target_metric_level.toLocaleString()} ${item.metricUnit}`], ['Target period', item.target_period], ['Minimum monthly gift', `$${item.min_monthly_contribution.toLocaleString()}`], ['Cost per unit', `$${item.cost_per_unit.toLocaleString()}`], ['Order', item.order?.toString() || 'Not set'], ['Created', new Date(item.createdAt).toLocaleString()], ['Updated', new Date(item.updatedAt).toLocaleString()]].map(([label, value]) => <div key={label} className="rounded-xl bg-editorial-charcoal/5 p-3"><dt className="text-[9px] font-bold uppercase text-editorial-charcoal/45">{label}</dt><dd className="mt-1 text-xs">{value}</dd></div>)}</dl></div></section>
    <section className="space-y-4"><div><h3 className="font-serif text-2xl">Subscribers & commitments</h3><p className="text-xs text-editorial-charcoal/50">Durable payment history and current recurring subscription state.</p></div><AdminListState loading={commitments.isLoading} error={commitments.error} empty={!commitments.data?.commitments.length}><div className="overflow-x-auto rounded-2xl border border-editorial-charcoal/10"><table className="min-w-[1100px] w-full border-collapse text-left text-xs"><thead className="bg-editorial-charcoal text-editorial-cream"><tr>{['Subscriber', 'Commitment', 'Amount', 'Status', 'Subscription details', 'Payment requests', 'Dates'].map(label => <th key={label} className="px-4 py-3 text-[9px] uppercase tracking-wider">{label}</th>)}</tr></thead><tbody className="divide-y divide-editorial-charcoal/10 bg-editorial-card">{commitments.data?.commitments.map(commitment => <tr key={commitment.id} className="align-top"><td className="px-4 py-4"><p className="font-bold">{commitment.user ? `${commitment.user.firstName} ${commitment.user.lastName || ''}` : 'Deleted user'}</p><p className="mt-1 text-[10px] text-editorial-charcoal/50">{commitment.user?.email || commitment.userId}</p></td><td className="px-4 py-4"><p className="font-bold capitalize">{commitment.type}</p><p className="mt-1 font-mono text-[9px] text-editorial-charcoal/45">{commitment.id}</p></td><td className="px-4 py-4 font-bold">{formatMinor(commitment.amountMinor, commitment.currency)}<p className="mt-1 text-[10px] font-normal capitalize text-editorial-charcoal/50">{commitment.interval || 'one time'}</p></td><td className="px-4 py-4 capitalize">{commitment.status}</td><td className="px-4 py-4"><SubscriptionDetails id={commitment.subscription?.id} stripeId={commitment.subscription?.stripeSubscriptionId} /></td><td className="px-4 py-4"><div className="space-y-2">{commitment.paymentRequests.length ? commitment.paymentRequests.map(request => <div key={request.id}><p className="font-bold capitalize">{request.status}</p><p className="font-mono text-[9px] text-editorial-charcoal/45">{request.id}</p>{request.stripePaymentIntentId && <p className="text-[9px] text-editorial-charcoal/45">Intent: {request.stripePaymentIntentId}</p>}{request.stripeCheckoutSessionId && <p className="text-[9px] text-editorial-charcoal/45">Session: {request.stripeCheckoutSessionId}</p>}</div>) : <span className="text-editorial-charcoal/40">None</span>}</div></td><td className="px-4 py-4 text-[10px]"><p>Created: {new Date(commitment.createdAt).toLocaleString()}</p>{commitment.completedAt && <p>Completed: {new Date(commitment.completedAt).toLocaleString()}</p>}{commitment.cancelledAt && <p>Cancelled: {new Date(commitment.cancelledAt).toLocaleString()}</p>}<p>Updated: {new Date(commitment.updatedAt).toLocaleString()}</p></td></tr>)}</tbody></table></div></AdminListState>{commitments.data && <AdminPagination page={commitments.data.page} limit={commitments.data.limit} total={commitments.data.total_commitments} onChange={update} />}</section>
  </div>;
}

function SubscriptionDetails({ id, stripeId }: { id?: string; stripeId?: string | null }) {
  const detail = useSubscription(id || '', !!id);
  if (!id) return <span className="text-editorial-charcoal/40">One-time commitment</span>;
  if (detail.isLoading) return <span>Loading subscription…</span>;
  if (detail.error || !detail.data) return <div><p className="text-rose-600">Details unavailable</p><p className="font-mono text-[9px] text-editorial-charcoal/45">{id}</p></div>;
  const item = detail.data;
  return <div className="space-y-1"><p><strong>{item.subscriptionStatus}</strong> · access {item.accessStatus}</p><p>{item.amount == null ? 'Amount pending' : `$${item.amount.toLocaleString()} ${item.currency?.toUpperCase()}`} {item.interval ? `/ ${item.interval}` : ''}</p><p>Cancellation: {item.cancellation.status}</p><p>Billing review: {item.billingReviewStatus}</p>{item.currentPeriodEnd && <p>Period ends: {new Date(item.currentPeriodEnd).toLocaleString()}</p>}<p className="font-mono text-[9px] text-editorial-charcoal/45">{id}</p>{stripeId && <p className="font-mono text-[9px] text-editorial-charcoal/45">Stripe: {stripeId}</p>}</div>;
}

function formatMinor(amount: number, currency: string) {
  try { return new Intl.NumberFormat(undefined, { style: 'currency', currency: currency.toUpperCase() }).format(amount / 100); } catch { return `${(amount / 100).toFixed(2)} ${currency.toUpperCase()}`; }
}
